export enum JsonValueType {
  none,
  null,
  undefined,
  boolean,
  number,
  string,
  array,
  object,
  function,
}

export enum Type {
  primary = "primary",
  secondary = "secondary",
  default = "default"
}

export enum Size {
  small = "small",
  medium = "medium",
  default = "default",
  large = "large",
}
